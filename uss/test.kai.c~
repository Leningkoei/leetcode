#include <stddef.h>

typedef struct cons {
  int current;
  struct cons *rest;
} *Cons;
Cons cons(int current, Cons rest) {
  Cons this = (Cons)malloc(sizeof(Cons));
  this->current = current;
  this->rest = rest;
  return this;
};
int main() {
  Cons list = cons(0, NULL);
  return 0;
};
